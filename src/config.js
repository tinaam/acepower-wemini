/* ========================================================
                        some config file
======================================================== */
let domain = 'http://127.0.0.1:8090'
let baseUrl = `${domain}/api/v1`
let source = 'wx'

export default{
  domain,
  baseUrl,
  source
}