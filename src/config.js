/* ========================================================
                        some config file
======================================================== */

let domain = 'http://127.0.0.1:8090'
// let domain = 'https://wxapp.thunf.cn'
let baseUrl = `${domain}/api/v1`
let source = 'wx'

export default{
  domain,
  baseUrl,
  source
}